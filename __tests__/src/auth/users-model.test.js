'use strict';

describe('user-model', ()=> {
  it('should populate the acl table with user before findOne', () => {
    expect(1).toEqual(1);
  });

  it('hash password before save', () => {
    expect(1).toEqual(1);
  });

  it('should create authorized from google oauth', () => {
    expect(1).toEqual(1);
  });

  it('should check for token existence', () => {
    expect(1).toEqual(1);
  });

  it('should check user presence in database and using correct associated password', () => {
    expect(1).toEqual(1);
  });

  it('should check to see if current token is valid', () => {
    expect(1).toEqual(1);
  });

  it('should compare password passed in to one in db', () => {
    expect(1).toEqual(1);
  });

  it('should generate token with associated  capability', () => {
    expect(1).toEqual(1);
  });

  it('should check if a capability has a certain role', () => {
    expect(1).toEqual(1);
  });

  it('should generate a key', () => {
    expect(1).toEqual(1);
  });
});
